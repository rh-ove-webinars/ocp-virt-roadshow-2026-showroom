[inventory]
# Required so Automation Controller's SCM-sourced inventory sync (see
# automation/vm-content/inventory/vms.kubernetes.yml) is allowed to use the
# kubernetes.core.k8s inventory plugin when it checks out this repo.
enable_plugins = kubernetes.core.k8s, auto
